# ems-insyte-generator

Типы генераторов:
  * Случайный (реализован)
  * Аналоговый (на уровне комбинации примитивных операций (знаки: +,-,/,*))
  * Цифровой (реализован)

Настройки генератора задаются в конфигурационных файлах "*_generator-config.json"